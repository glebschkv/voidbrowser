{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "sudo apt-get update -qq && sudo apt-get install -y -qq libgtk-3-dev libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev libsoup-3.0-dev > /tmp/deps-install.log 2>&1 && echo 'GTK/WebKit deps installed' || echo 'Failed to install deps, check /tmp/deps-install.log'",
        "timeout": 300000
      }
    ]
  }
}
